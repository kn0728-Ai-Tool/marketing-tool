# ======================================================
# requirements.txt  - AIキーワード分析ツール 完全版
# ======================================================

# ── Web UI ──
streamlit>=1.32.0

# ── AI / OpenAI ──
openai>=1.12.0

# ── データ処理 ──
pandas>=2.0.0
numpy>=1.26.0

# ── グラフ描画 ──
plotly>=5.18.0

# ── Google Trends ──
pytrends>=4.9.2

# ── PDF読み込み（文章要約タブ）──
pypdf>=3.0.0

# ── Word（.docx）読み込み（文章要約タブ）──
python-docx>=1.0.0

# ── HTTP通信（pytrends依存）──
requests>=2.31.0

# ── タイムゾーン（Python 3.9未満の環境用・3.9以上は標準搭載のため任意）──
# backports.zoneinfo>=0.2.1

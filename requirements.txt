# ======================================================
# requirements.txt  - AIキーワード分析ツール 完全版 v3.2
# ======================================================

# ── Web UI ──
streamlit>=1.32.0

# ── AI / OpenAI ──
openai>=1.12.0

# ── データ処理 ──
pandas>=2.0.0
numpy>=1.26.0

# ── グラフ描画 ──
plotly>=5.18.0

# ── Google Trends ──
pytrends>=4.9.2

# ── PDF読み込み（文章要約・ダイジェストタブ）──
pypdf>=3.0.0

# ── Word（.docx）読み込み（文章要約・ダイジェストタブ）──
python-docx>=1.0.0

# ── PowerPoint生成（ダイジェストタブ）──
python-pptx>=0.6.21

# ── Excel生成（ダイジェストタブ）──
openpyxl>=3.1.0

# ── HTTP通信（pytrends依存）──
requests>=2.31.0
